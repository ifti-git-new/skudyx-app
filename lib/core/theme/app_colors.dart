import 'package:flutter/material.dart';

abstract class AppColors {
  // Base
  static const primary = Color(0xFF061640);
  static const bg = Colors.white;

  static const textPrimary = Color(0xFF111827);
  static const textSecondary = Color(0xFF6B7280);

  static const border = Color(0xFFE5E7EB);
  static const surface = Colors.white;

  static const warning = Color(0xFFFFC107);
  static const danger = Color(0xFFE53935);

  // Splash gradient (from your design)
  static const splashTop = Color(0xFFF2FFF7); // very light mint
  static const splashMid = Color(0xFF7EB7FF); // light blue
  static const splashBottom = Color(0xFF4D93FF); // deeper blue
}
