import 'package:flutter/material.dart';

class EmergencyContactScreen extends StatelessWidget {
  const EmergencyContactScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Emergency Contact (Placeholder)')),
      body: const Center(
        child: Text(
          'Emergency contact add/edit/verify UI will go here (Figma).',
        ),
      ),
    );
  }
}
