import 'package:flutter/material.dart';

class DeliveryConfirmationScreen extends StatelessWidget {
  const DeliveryConfirmationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Delivery Confirmation (Placeholder)')),
      body: const Center(
        child: Text('Order placed confirmation UI will go here (Figma).'),
      ),
    );
  }
}
