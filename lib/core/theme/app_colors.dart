import 'package:flutter/material.dart';

abstract class AppColors {
  // Brand
  static const primary = Color(0xFF081B4A);

  // Surfaces
  static const bg = Color(0xFFF7F8FA);
  static const surface = Colors.white;

  // Text
  static const text = Color(0xFF111827);
  static const muted = Color(0xFF6B7280);
  static const hint = Color(0xFF9AA3AF);

  // Lines
  static const border = Color(0xFFE5E7EB);

  // Status
  static const success = Color(0xFF16A34A);
  static const successSoft = Color(0xFFDFF7DF);

  static const warning = Color(0xFFF59E0B);
  static const warningSoft = Color(0xFFFFE9A6);

  static const danger = Color(0xFFDC2626);
  static const dangerSoft = Color(0xFFFFE4E6);

  static const info = Color(0xFF38BDF8);
  static const accent = Color(0xFF4FD3E6);

  // Splash gradient
  static const splashTop = Color(0xFFF2FFF7);
  static const splashMid = Color(0xFF7EB7FF);
  static const splashBottom = Color(0xFF4D93FF);

  // Headers gradient
  static const headerGrad1 = Color(0xFF6CA8FF);
  static const headerGrad2 = Color(0xFF2F78FF);
}
